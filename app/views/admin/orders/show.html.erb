<body class="d-flex h-100 text-left text-black bg-light">
  <main class="px-3">


    <div class = "toptext">
  <h4 style = "color: green;">注文履歴一覧</h4></div>
  <div class="col-md-8 offset-md-1">
<table class="table">

    <tr>
      <th>購入者</th>
      <th>注文日</th>
      <th>配送先</th>
      <th>支払方法</th>
      <th>注文ステータス</th>


<!--<table class="table">-->
    <tr>
      <td><%= @order.name %></td>
      <td><%= @order.created_at.strftime('%Y/%m/%d') %></td>
      <td><%= @order.postal_code %><%= @order.address %></td>

      <td><%= @order.pay_method %></td>

      <td><%= @order.order_status %></td>




<table class="table">

    <tr>
      <th>商品名</th>
      <th>単価（税込）</th>
      <th>数量</th>

      <th></th>
      <th>小計</th>

    </tr>
<% @order_items.each do |order_item| %>
<tr>
      <td><%= order_item.item.name %></td>
      <td>￥<%= (order_item.item.price*1.10).round %></td>
      <td><%= order_item.amount %></td>
      <td><%= ((order_item.price*order_item.amount)*1.10).round %></td>

</tr>
<!--cart_item_path(cart_item)-->

<% end %>
      </td>
</div>
</table>





  </main>


</div>



  </body>
</html>



